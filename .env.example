# Application
APP_NAME=Bokit
APP_SLOGAN="Bring On Kitsch Island Time"
APP_LOGO=🏖️
APP_KEY=
APP_DEBUG=true
APP_ENV=local

# Authentication
# Method: "none" (no authentication) or "wordpress" (WordPress integration)
AUTH_METHOD=none

# WordPress Authentication (required if AUTH_METHOD=wordpress)
WP_SITE_URL=https://example.com
WP_REQUIRED_ROLE=administrator

# Database (auto-configured by default to use SQLite)
DB_CONNECTION=sqlite

# Session & Cache (file-based by default, no config needed)
SESSION_DRIVER=file
CACHE_STORE=file
